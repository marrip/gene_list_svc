package main

var tables = map[string]string{
	"entity": "id varchar(20) NOT NULL, class varchar(10) NOT NULL, snv boolean, cnv boolean, sv boolean, pindel boolean, list_aml boolean, list_aml_ext boolean, list_all boolean, PRIMARY KEY (id)",
}
